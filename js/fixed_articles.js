              /* jQuery(function ($) {
                   $(document).ready(function () {
                       $('#list').stickUp();
                   });
               });
               
*/
/*$(function(){
$(window).scroll(function() {
var top = $(document).scrollTop();
   if (top <= 300) $("#list").css({top: '0', position: 'relative'});
   else $("#list").css({top: '10', position: 'fixed'});
    
  });
});

        */   /*     
                $(function () {
                    $(window).scroll(function() {

                    var handleMatchMedia = function (mediaQuery) {
                            if (mediaQuery.matches) {
                                (top <= 150) $("#list").css({top: '0', position: 'relative'});
                            } else {
                                $("#list").css({top: '0', position: 'fixed'});
                            }
                        },
                        mql = window.matchMedia('all and (min-width: 1080px)');

                    handleMatchMedia(mql);
                    mql.addListener(handleMatchMedia);
                        });

                });*/

           

